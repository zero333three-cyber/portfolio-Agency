# ZeroThree Solutions
## Deployed :- https://portfolio-agency-client.vercel.app/
Premium AI-focused agency portfolio built as a modern MERN web app.

## Features

- Dark, futuristic SaaS landing page
- AI services, portfolio, testimonials, contact form
- Dark/light mode toggle
- Floating WhatsApp CTA
- Responsive, glassmorphism UI
- Express backend with contact endpoint

## Run locally

```bash
npm install
npm run dev
```

Open the client app at `http://localhost:5173`.

## Build

```bash
npm run build
```

## Backend

The backend server runs on port `4000` and exposes `/api/contact`.
